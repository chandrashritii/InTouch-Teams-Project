{
  "name": "WebRTC-server",
  "version": "0.0.1",
  "description": "",
  "main": "app.js",
  "engines": {
    "node": "10.24.1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "serve-favicon": "^2.5.0",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.9"
  },
  "scripts": {
    "start": "nodemon --watch \"/\" --exec \"node app.js\"",
    "heroku-prebuild": "npm i -f",
    "heroku-postbuild": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "LiveStream",
    "Server"
  ]
}
